/**
 * Supabase browser config (safe to commit).
 *
 * For Vercel deployment, set SUPABASE_URL and SUPABASE_ANON_KEY as Environment Variables.
 * The browser will read them via `GET /api/public-config`.
 *
 * Supabase → Project Settings → API:
 * - Project URL
 * - anon public key
 */
export const SUPABASE_URL = '';
export const SUPABASE_ANON_KEY = '';

