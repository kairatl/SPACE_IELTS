/**
 * AI config (safe to commit).
 *
 * IMPORTANT:
 * - Never commit real API keys.
 * - For Vercel, set OPENAI_API_KEY in Environment Variables (server-side),
 *   and the client will call `/api/ai` without exposing the key.
 */
export const OPENAI_API_KEY = '';

